/*
Package term provides functions for dealing with POSIX compliant
terminals/terminal emulators that also support ANSI escape sequences.

It is only tested on Linux with the Xfce terminal emulator and the Linux console.

All inputs can be canceled with ^D (EOF).
*/
package term
